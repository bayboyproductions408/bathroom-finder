android.useAndroidX=true
org.gradle.jvmargs=-Xmx2048m
# ---------------------------------------------------------------------------
# The one thing you must change: where the app actually lives.
# A Trusted Web Activity loads your real site, so this has to be a public
# https host — not a LAN address, not localhost.
# ---------------------------------------------------------------------------
twaHost=bathroomfinder.example.com
twaStartPath=/
